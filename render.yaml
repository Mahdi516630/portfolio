services:
  - type: web
    name: portfolio-mahdi-yacoub-ali
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
